base: base.yaml
model:
  experiment: m2
train:
  batch_size: 24
  gradient_accumulation: 2
  learning_rate: 0.0002
  weight_decay: 0.0001
output:
  directory: outputs/m2
evaluation:
  batch_size: 24

